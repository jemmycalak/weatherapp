<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:paddingTop="@dimen/marginTop_date"
    android:paddingBottom="@dimen/marginTop_date"
    android:paddingLeft="@dimen/marginLeft_date"
    android:paddingRight="@dimen/marginLeft_date">

    <TextView
        android:id="@+id/date"
        android:text="Selasa"
        style="@style/text_hours_dashboard"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toLeftOf="@+id/cloudDate"/>

    <ImageView
        android:id="@+id/cloudDate"
        android:src="@drawable/ic_cloud_cloudy"
        app:layout_constraintRight_toLeftOf="@+id/tempDate"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="@dimen/marginLeft_cloud_date"/>
    <TextView
        android:id="@+id/tempDate"
        style="@style/text_celcius_hours_dashboard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>